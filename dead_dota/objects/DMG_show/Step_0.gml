/// @description Insert description here
// You can write your code in this editor
/*x += sin(delta++ / 10) * 1.1;*/
if (alpha = true) {
	image_alpha -= 0.03;
	image_xscale -= 0.01
	y -= 0.5;
}
else {
	y -= 10;
	image_xscale += 0.03
}