if (startment = 1) {
	x += lengthdir_x(28 * cid_id.big_val,image_angle-90);
	y += lengthdir_y(28 * cid_id.big_val,image_angle-90);
	deltx += 0.3;
	deltp -= 0.05;
}
else {
	image_xscale += 0.075;
	image_yscale += 0.075;	
}