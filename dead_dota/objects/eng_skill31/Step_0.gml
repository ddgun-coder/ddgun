/// @description Insert description here
if (dis > 0) {
	dis--;	
}
x += lengthdir_x(dis, dir);
y += lengthdir_y(dis, dir);


if (effect = true) {
	image_angle += irandom_range(0, 50)
	image_alpha += 0.05
}