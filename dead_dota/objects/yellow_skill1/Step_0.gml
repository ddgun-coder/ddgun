if (startment = 1) {
	x += lengthdir_x(18 * cid_id.big_val,cid_id.YA-90);
	y += lengthdir_y(18 * cid_id.big_val,cid_id.YA-90);
	deltx += 0.3;
	deltp -= 0.05;
}
else {
	image_xscale += 0.05;
	image_yscale += 0.05;	
}