 x = xstart + camera_get_view_x(view_camera[0])
 y = ystart + camera_get_view_y(view_camera[0])
switch(room) {
	default:
	if (global.hat = spr_hat72) {
		if (collision_point(mouse_x, mouse_y, id, true, false)) {
		    draw_sprite_ext(spr_snowbox, testing.bullet , xstart, ystart,1,1,0,c_gray,1)
		}
		else{
		    draw_sprite(spr_snowbox, testing.bullet, xstart, ystart)
		}
	}
	break;
}