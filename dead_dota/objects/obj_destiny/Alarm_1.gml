/// @description Insert description here
// You can write your code in this editor
if (timeline_index != ik_skill4) {
	with(testing) {
		a_possible = true;
		sturn = 200;
		normal_play(cry_1_act);
	}
}
camera_set_view_target(view_camera[0], cid_id.id);