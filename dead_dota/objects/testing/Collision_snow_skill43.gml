/// @description Insert description here
// You can write your code in this editor
if !(other.team == global.team) {
    all_YA_change(other.image_angle)
    if (place_free(other.x, other.y)) {
	    x = other.x;
	    y = other.y;
    }
}