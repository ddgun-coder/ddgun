if (other.team != global.team) {
	if (arm_type_more != "a_w") {
		buff_index = spr_gas;
		buff_time = 320
		server77_equal(serve_val.buff_index, spr_none, buffer_u16);
		DMG(70, n_attack_area, 60);
		other_cid = 0;
	}
}