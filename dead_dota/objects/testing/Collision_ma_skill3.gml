if (global.my_cid_id != other.cid_id) {
    if ( other.image_alpha = 1) {
        var ma_power = other.ma_power * 0.35
        heal_server( ma_power / 80);
    }
}
else {
	if ( other.image_alpha = 1) {
        var ma_power = other.ma_power * 0.35
        heal_server( ma_power / 120);
    }
}