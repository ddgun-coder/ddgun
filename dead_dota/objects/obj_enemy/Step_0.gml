/// @description Insert description here
// You can write your code in this edito
/*if (sturn <= 0) {
	var _inst = instance_place(x, y, obj_dmgable);
	if _inst != noone
	    {
			if (_inst.cid == global.my_cid) {
				buffer_seek(buff_chat, buffer_seek_start, 0);
			
				buffer_write(buff_chat, buffer_u8, 113);
				buffer_write(buff_chat, buffer_u16, _inst.dmg);
				
				network_send_packet(0,buff_chat,3);
			}
	    }
}
else {
	sturn--;	
}
if (hp <= 0) {
	instance_destroy()	
}*/